name: Workflow Repository Dispatch
on: 
  repository_dispatch:
    types: [test-event]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Building the project..."
